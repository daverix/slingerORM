apply plugin: 'java'
apply plugin: 'maven'

sourceCompatibility = 1.6

uploadArchives {
    repositories {
        mavenDeployer {
            repository url: 'file://' + new File(getRootDir(), 'pkg').absolutePath
            pom.artifactId = 'slingerorm-core'
        }
    }
}

